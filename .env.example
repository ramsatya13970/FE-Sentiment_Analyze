# Base URL of the CX Insights API (no trailing slash).
# Leave blank to call /api/v1/... as a same-origin / proxied path.
VITE_API_BASE_URL=

# Default location_id to preload on first render.
VITE_DEFAULT_LOCATION_ID=6b486d79-9fc7-4f35-bf6f-e037e3e10e0d
